; @vercel/analytics declares optional framework peers (e.g. @sveltejs/kit)
; that transitively demand vite 8, conflicting with vitest's vite 7. We use
; none of those frameworks, so accept the existing resolution. Without this,
; `npm install` (incl. Vercel's build step) fails with ERESOLVE.
legacy-peer-deps=true
