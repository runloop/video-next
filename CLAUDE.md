@AGENTS.md

## Agent skills

### Issue tracker

Issues are tracked as GitHub issues on `runloop/video-next` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical default triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`); created on demand. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
